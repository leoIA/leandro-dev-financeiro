# Bloquear acesso web direto aos certificados digitais A1 (.pfx/.p12)
# Compatível com Apache 2.2 (Order/Deny) e 2.4+ (Require all denied)
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
